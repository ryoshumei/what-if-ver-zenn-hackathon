# ==============================
# Core Google Cloud
# ==============================
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# ==============================
# Vertex AI Models
# ==============================
# Vertex AI Model Configuration
VERTEX_CHAT_MODEL=gemini-2.5-flash
VERTEX_PLAN_MODEL=gemini-2.5-flash
VERTEX_IMAGE_MODEL=imagen-4.0-generate-001
VERTEX_VIDEO_MODEL=veo-3.0-fast-generate-001

# Video LRO mode: mock | sdk | rest
VERTEX_VIDEO_LRO_MODE=rest

# ==============================
# Storage
# ==============================
# Where Veo writes outputs (Vertex response); must be a gs:// URI
# e.g. gs://your-private-bucket/outputs/
GCS_OUTPUT_BUCKET=gs://your-private-bucket/outputs/

# Optional: public bucket name (NOT a gs:// URI) to host final files
# App will copy artifacts here and make them public, returning HTTPS URLs
# e.g. what-if-ver-zenn-hackathon-videos-public
PUBLIC_GCS_BUCKET=

# ==============================
# Firebase Client (public)
# ==============================
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id

# ==============================
# Auth (optional)
# ==============================
# For local-only Edge streaming route (/api/vertex/stream) if you choose to use it
# Normally we rely on ADC; leave empty to disable.
ACCESS_TOKEN=

# Node environment
NODE_ENV=development

# ==============================
# Local developer note (not required in production):
# For ADC in local dev, point GOOGLE_APPLICATION_CREDENTIALS to your SA key JSON.
# export GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/vertex-sa.json
# ==============================

